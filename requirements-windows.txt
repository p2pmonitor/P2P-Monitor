# P2P Monitor — Windows build dependencies
# Install with: pip install -r requirements-windows.txt

# Process/file handle detection (cross-platform, replaces /proc on Windows)
psutil>=5.9.0

# Window screenshot capture (Windows backend for capture_window_image)
Pillow>=10.0.0

pystray>=0.19.0

# Stats tab charting (Checkpoint 2, v2.0.0) — FigureCanvasTkAgg embed
matplotlib>=3.7.0

# Discord bot integration (must be bundled — not installed at runtime in packaged builds)
discord.py>=2.3.0
